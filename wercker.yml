box: golang
dev:
  steps:
    - internal/watch:
        code: |
          go build ./...
          ./git2go-example
        reload: true
# Build definition
build:
  # The steps that will be executed on build
  steps:

    # Build the project
    - script:
        name: go build
        code: |
          go build ./...
    # Test the project
    - script:
        name: go test
        code: |
          go test ./...
